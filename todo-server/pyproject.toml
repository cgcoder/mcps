[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "todo-server"
version = "0.1.0"
description = "Personal todo-list MCP server"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.0.0",
]

[project.scripts]
todo-server = "server:main"

[tool.hatch.build.targets.wheel]
packages = ["."]
